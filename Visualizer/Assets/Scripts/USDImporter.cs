using UnityEngine;
//using Omniverse; // Asegúrate de usar el namespace correcto para Omniverse

public class USDImporter : MonoBehaviour
{
    /*public string usdFilePath; // Ruta del archivo USD a importar

    void Start()
    {
        ImportUSDModel(usdFilePath);
    }

    public void ImportUSDModel(string path)
    {
        if (string.IsNullOrEmpty(path))
        {
            Debug.LogWarning("USD file path is empty or null.");
            return;
        }

        // Usa la API de Omniverse para cargar el archivo USD
        try
        {
            Omniverse.USD.LoadUSD(path, (usdRootObject) =>
            {
                if (usdRootObject != null)
                {
                    // Manipula el objeto USD importado como necesites
                    Debug.Log("USD model loaded successfully.");
                }
                else
                {
                    Debug.LogWarning("Failed to load USD model.");
                }
            });
        }
        catch (System.Exception ex)
        {
            Debug.LogError("Error importing USD model: " + ex.Message);
        }
    }*/
}
